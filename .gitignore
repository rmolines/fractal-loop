# Fractal Loop tree (each project has its own)
.fractal/

# Session lock files (transient, managed by session-lock.sh)
**/session.lock

# Claude Code runtime (worktrees, agents — not source code)
.claude/

# OS
.DS_Store

# Editor
*.swp
*.swo
*~
